#! /bin/bash

# This is intended to be a temporary directory
# for use by multiple docker containers. Without
# this, a container will run out of disk space
# and ingest will fail.

echo "Making temporary directory for lcmap-data project"
mkdir /data/lcmap