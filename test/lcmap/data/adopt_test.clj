(ns lcmap.data.adopt-test
  (:require [clojure.test :refer :all]
            [lcmap.data.ingest :refer :all]
            [lcmap.data.shared-test :as shared]
            [gdal.core]
            :reload))
